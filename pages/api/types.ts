export type PriceType = Array<{
  id: number;
  title: string;
  price: string;
  description: String[];
}>;

export interface IPriceType {
  id: number;
  title: string;
  price: string;
  description: String[];
}
